# Environment Variables

Before starting the application, there are some environment variables that should be set in a `.env`. The following is an example of the variables:

```conf
VITE_API_URL=
```

## VITE_API_URL

__Required__.

The host of the back-end application. It is necessary to request all the data of the application.
